{% extends 'base.html.twig' %}

{% block title %}TypeProduits{% endblock %}

{% block body %}
    <h1>TypeProduits</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_produit.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_produit.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_produits_index') }}">back to list</a>

    <a href="{{ path('type_produits_edit', {'id': type_produit.id}) }}">edit</a>

    {{ include('type_produits/_delete_form.html.twig') }}
{% endblock %}
