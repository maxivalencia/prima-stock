{% extends 'base.html.twig' %}

{% block title %}Type Produit{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Type de Produit</h1>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_produit.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_produit.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_produits_index') }}">Retour</a>

    <a href="{{ path('type_produits_edit', {'id': type_produit.id}) }}">Edition</a>

    {{ include('type_produits/_delete_form.html.twig') }}
</div>
{% endblock %}
