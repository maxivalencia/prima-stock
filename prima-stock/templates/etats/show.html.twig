{% extends 'base.html.twig' %}

{% block title %}Etats{% endblock %}

{% block body %}
    <h1>Etats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etat.id }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ etat.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etats_index') }}">back to list</a>

    <a href="{{ path('etats_edit', {'id': etat.id}) }}">edit</a>

    {{ include('etats/_delete_form.html.twig') }}
{% endblock %}
