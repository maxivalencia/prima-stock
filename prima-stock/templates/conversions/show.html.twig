{% extends 'base.html.twig' %}

{% block title %}Conversions{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Conversions</h1>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conversion.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ conversion.valeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('conversions_index') }}">Retour</a>

    <a href="{{ path('conversions_edit', {'id': conversion.id}) }}">Edition</a>

    {{ include('conversions/_delete_form.html.twig') }}
</div>
{% endblock %}
