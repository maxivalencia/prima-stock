{% extends 'base.html.twig' %}

{% block title %}Conversions Liste{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Conversion</h1>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Valeur</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for conversion in conversions %}
            <tr>
                <td>{{ conversion.id }}</td>
                <td>{{ conversion.valeur }}</td>
                <td>
                    <a href="{{ path('conversions_show', {'id': conversion.id}) }}">voir</a>
                    <a href="{{ path('conversions_edit', {'id': conversion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouver!</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('conversions_new') }}">Créer</a>
</div>
{% endblock %}
