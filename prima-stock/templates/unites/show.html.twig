{% extends 'base.html.twig' %}

{% block title %}Unites{% endblock %}

{% block body %}
    <h1>Unites</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ unite.nom }}</td>
            </tr>
            <tr>
                <th>Sigle</th>
                <td>{{ unite.sigle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unites_index') }}">back to list</a>

    <a href="{{ path('unites_edit', {'id': unite.id}) }}">edit</a>

    {{ include('unites/_delete_form.html.twig') }}
{% endblock %}
