{% extends 'base.html.twig' %}

{% block title %}Validations{% endblock %}

{% block body %}
    <h1>Validations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ validation.id }}</td>
            </tr>
            <tr>
                <th>Validation</th>
                <td>{{ validation.validation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('validations_index') }}">back to list</a>

    <a href="{{ path('validations_edit', {'id': validation.id}) }}">edit</a>

    {{ include('validations/_delete_form.html.twig') }}
{% endblock %}
