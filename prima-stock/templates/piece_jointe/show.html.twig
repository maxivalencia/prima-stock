{% extends 'base.html.twig' %}

{% block title %}PieceJointe{% endblock %}

{% block body %}
    <h1>PieceJointe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ piece_jointe.id }}</td>
            </tr>
            <tr>
                <th>NomFichier</th>
                <td>{{ piece_jointe.nomFichier }}</td>
            </tr>
            <tr>
                <th>NomServer</th>
                <td>{{ piece_jointe.nomServer }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ piece_jointe.reference }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('piece_jointe_index') }}">back to list</a>

    <a href="{{ path('piece_jointe_edit', {'id': piece_jointe.id}) }}">edit</a>

    {{ include('piece_jointe/_delete_form.html.twig') }}
{% endblock %}
